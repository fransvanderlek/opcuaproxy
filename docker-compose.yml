version: "3"
services:
  opcua-proxy:
    image: opcua-proxy:latest
    volumes:
      - "./internal:/internal"
      - "./config:/config"
    networks:
      - proxynet
    ports:
      - "12685:12685"

networks:
  proxynet:
