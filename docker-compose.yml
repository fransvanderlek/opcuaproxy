version: "3"
services:
  mqtt-broker:
    image: intelligentindustry:opcuaproxy-0.1
    volumes:
      - "./internal:/internal"
      - "./config:/config"
    networks:
      - localnet
    ports:
      - "12685:12685"

networks:
  localnet:
